<h1>Post List</h1>

<table class="table-bordered">
  <thead class="bg-dark">
    <tr>ID</tr>
    <tr>Title</tr>
    <tr>Content</tr>
    <tr>User</tr>
    <tr>Action</tr>
  </thead>
    <tr>
      <td><%= post.id %></td>
      <td><%= post.title %></td>
      <td><%= post.content %></td>
      <td><%= post.login.email %></td>
      <td><%=  %></td>
    </tr>
</table>

<%= link_to 'New Post', new_post_path%>